#version 330 core
layout (location = 0) in vec3 pos;

out vec3 text_coord;

uniform mat4 projection;
uniform mat4 view;

void main() {
    text_coord = pos;
    gl_Position = vec4(pos, 1.0) * view * projection;
} 